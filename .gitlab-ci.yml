pages:
  image: fedora:latest
  script:
    - sudo dnf install -y unzip pandoc
    - mkdir public
    - mkdir public/docs
    - mv style.css public
    - ./fetch-docs.sh
  artifacts:
    paths:
      - public
  only:
    - pages

