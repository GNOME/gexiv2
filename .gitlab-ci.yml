pages:
  image: fedora:latest
  script:
    - sudo dnf install -y unzip pandoc
    - mkdir public
    - mkdir public/docs
    - cp style.css public
    - mv style.css public/docs
    - ./fetch-docs.sh
  artifacts:
    paths:
      - public
  only:
    - pages

